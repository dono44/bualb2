import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dualaudio.bluetoothstudio',
  appName: 'Dual Audio Bluetooth Studio',
  webDir: 'dist'
};

export default config;
